# QazZerep PRO

QazZerep PRO — платформа для сравнения студенческих работ (PDF/DOCX/TXT) с визуализацией совпадений, семантической оценкой и публичной верификацией отчётов.

[UI Preview](frontend/index.html)

---

## 🚀 О проекте

Проект автоматизирует проверку уникальности и снижает риск списывания за счёт комбинирования лексического и семантического анализа, а также оценки вероятности AI‑генерации текста.

**Ключевые идеи:**
- Быстрое пакетное сравнение нескольких файлов
- Визуальный дифф с подсветкой совпадений
- Семантическое сходство через LaBSE эмбеддинги
- Публичные отчёты по ссылке / QR‑коду
- Профили пользователей с фильтрами обработки

---

## 🖥️ Функционал (по текущему коду)

- Загрузка нескольких файлов для сравнения (PDF, DOCX, TXT)
- Лексический дифф (SequenceMatcher) + семантическая близость (LaBSE)
- Формула итоговой схожести: 70% лексика + 30% семантика
- Генерация публичных отчётов с QR‑ссылкой на верификацию
- История сравнений пользователя
- Проверка вероятности AI‑текста (Zippy)
- Настройки фильтрации: GOST/APA/таблицы/титулы/цитаты + кастомные regex
- JWT‑авторизация, регистрация преподавателей с кодом школы
- Профиль пользователя: аватар, смена пароля, настройки

---

## 📦 Технологии

- **Frontend:** React, Vite, Tailwind CSS
- **Backend:** FastAPI
- **NLP:** sentence-transformers (LaBSE), NumPy
- **Парсинг файлов:** PyMuPDF (PDF), python-docx (DOCX)
- **AI‑детектор:** zippy
- **Database:** MongoDB (motor)
- **Auth:** JWT (PyJWT)

---

## ⚙️ Требования

- Python 3.10+
- Node.js 18+ (для разработки фронта)
- MongoDB (локально: mongodb://localhost:27017)

---

## ⚡ Быстрый старт (рекомендованный)

1. **Установите зависимости backend:**
   ```bash
   pip install -r requirements.txt
   ```
2. **Запустите MongoDB** (по умолчанию локально на 27017).
3. **Запустите backend (раздаёт готовый UI из app/front):**
   ```bash
   python3 main.py
   ```
4. **Откройте:**
   [http://localhost:8000](http://localhost:8000)

> При первом запуске модель LaBSE скачает веса (~1.8 GB).

---

## 🧪 Разработка фронтенда

```bash
cd frontend
npm install
npm run dev
```

> Для dev‑режима настройте прокси к API (порт 8000) или используйте собранный UI из app/front.

---

## 🔌 Основные API маршруты

- **Auth:**
  - `POST /auth/login`
  - `POST /auth/register`
  - `GET /auth/me`
  - `POST /auth/update-settings`
  - `POST /auth/upload-avatar`
  - `POST /auth/change-password`
- **Документы:**
  - `POST /documents/compare-batch`
  - `GET /documents/history`
- **Отчёты:**
  - `GET /reports/{report_id}`

---

## 🛡️ Данные и хранение

- Файлы обрабатываются в памяти и не сохраняются на диск.
- В MongoDB хранятся пользователи, история сравнений и отчёты.

---

## 🤝 Контакты и поддержка

- Telegram: [@brjxjxjd](https://t.me/brjxjxjd)
- Телефон: +7 (777) 123-45-67

---

**QazZerep PRO** — интеллектуальный щит против списывания.
